[package]
name = "pillar-quota-ledger"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Quorum-fenced reservation ledger over the streaming DB: reserves against a quota attestation budget before admitting a workload and refuses over-budget, CP-fenced through the one coordination core so two nodes cannot double-spend a shared budget."

[dependencies]
pillar-core = { path = "../pillar-core" }
pillar-coordination = { path = "../pillar-coordination" }
pillar-streamdb = { path = "../pillar-streamdb" }
pillar-trust-artifacts = { path = "../pillar-trust-artifacts" }

[lints]
workspace = true
