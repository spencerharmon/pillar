[package]
name = "pillar-ipam"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Quorum-fenced address allocation from a delegated pool: the Rust refinement of specs/IPAM.tla."

[dependencies]
pillar-core = { path = "../pillar-core" }
pillar-coordination = { path = "../pillar-coordination" }
pillar-topology = { path = "../pillar-topology" }

[dev-dependencies]
pillar-trust-artifacts = { path = "../pillar-trust-artifacts" }

[lints]
workspace = true
