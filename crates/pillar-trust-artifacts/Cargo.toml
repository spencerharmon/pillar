[package]
name = "pillar-trust-artifacts"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Four distinct content-addressed signed trust artifacts (certify/trust/attest/revoke) replacing any overloaded sign, refining specs/TrustArtifacts.tla; wired into pillar-rbac's single decider."

[dependencies]
pillar-crypto = { path = "../pillar-crypto" }
pillar-core = { path = "../pillar-core" }
pillar-rbac = { path = "../pillar-rbac" }

[lints]
workspace = true
