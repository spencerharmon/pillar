[package]
name = "pillar-eventlog"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "PGP-signed hash-linked event DAG over the content-addressed store: the Rust refinement of specs/EventDAG.tla."

[dependencies]
pillar-core = { path = "../pillar-core" }
pillar-streamdb = { path = "../pillar-streamdb" }
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
