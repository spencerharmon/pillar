# Rust
/target
**/*.rs.bk
Cargo.lock linguist-generated

# TLA+ / TLC
specs/.tools/
specs/states/
specs/*_TTrace_*.tla
specs/*_TTrace_*.bin
specs/*.old

# Editors / OS
.DS_Store
*.swp

# nix build output
/result

# pillar-integration image-under-test GC-root out-links (host-visible anchor
# under the checkout so the nix-daemon can resolve the indirect root even inside
# the bwrap DoD check-sandbox; see scripts/pillar-integration/lib/image.sh)
/.pillar-it-gcroot.*
